---
const { name, link, classProp } = Astro.props;
---
<a href={ link }>
    <button class={ classProp } type="button"> 
        { name }
    </button>
</a>

<style>
    *{
        color: #cedce9;
        font-size: 15px;
    }
    .buttonBordered{
        background: transparent;
        border-radius: 10px;
        padding: 5px;
        border: 2px #4e7ea9 solid;
        cursor: pointer;
        transition: all 0.3s linear;
        max-width: 150px;
        margin-right: 5px;
        font-size: 20px;

    }

    .buttonNormal{
        background: transparent;
        padding: 10px;
        cursor: pointer;
        transition: all 0.3s linear;
        max-width: 150px;
        margin-right: 5px;
        border: 0;
        font-size: 20px;
        height: fit-content;
    }
    
    button:hover   {
        border: var(--text) solid 1px;
    }
    a{
        text-decoration: none;
    }

</style>